
1. CORS 해결 dev_6

2.아래의 환경 파일 install
pip install django-environ

3.개발시 아래의 명령어로 실행
python manage.py runserver --settings=config.settings.dev

참고)
requirements.txt 생성
pip list --format=freeze > requirements.txt

###  전체 폴더 구조
django-react-nginx-hjs-distribute/
│
├── .github/
│   └── workflows/
│       └── deploy.yml       # GitHub Actions 워크플로우 파일
|
├── backend/                        # Django 프로젝트
│   ├── manage.py
│   ├── requirements.txt
│   ├── app/                       # Django 앱 소스코드들
│   └── ...                       
│
├── frontend/                       # Vite + React 프로젝트
│   ├── package.json
│   ├── vite.config.js
│   ├── public/
│   ├── src/
│   └── ...                       
│
├── nginx/                         # Nginx 설정 파일
│   └── default.conf               # Nginx 설정 (리버스 프록시 및 정적파일 서빙)
│
├── .gitignore
├── docker-compose.yml             # 도커 컴포즈 설정
└── README.md


docker exec -it 05 /bin/sh 
